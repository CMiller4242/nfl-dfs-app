# Production - required to run the pipeline and the Streamlit app.
streamlit>=1.61
pandas>=2.2
pyarrow>=15.0
plotly>=5.24
rapidfuzz>=3.9
nflreadpy>=0.1.5

# Testing - required to run `python -m pytest`.
pytest>=8.0
